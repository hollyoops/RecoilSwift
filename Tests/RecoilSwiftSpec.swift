import XCTest
import Quick
import Nimble

final class RecoilSwiftSpec: QuickSpec {
    override func spec() {
        describe("Hello word") {
            it("test hello word") {
                expect(1).to(equal(1))
            }
        }
    }
}
